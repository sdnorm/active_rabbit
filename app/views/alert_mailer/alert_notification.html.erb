<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ActiveRabbit Alert</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.5;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 8px 12px;
    }

    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 14px 16px 12px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }

    .header h1 {
      margin: 6px 0 2px;
      font-size: 18px;
    }

    .header p {
      margin: 0;
      font-size: 13px;
      opacity: 0.9;
    }
    .content {
      background: white;
      padding: 30px 20px;
      border: 1px solid #e5e5e5;
      border-top: none;
      border-radius: 0 0 8px 8px;
    }
    .alert-body {
      background: #f8f9fa;
      padding: 16px;
      border-radius: 6px;
      border-left: 4px solid #dc3545;
      margin: 16px 0;
      white-space: pre-line;
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 13px;
      line-height: 1.5;
    }
    .button {
      display: inline-block;
      padding: 10px 18px;
      background: #667eea;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      margin-top: 12px;
      font-size: 14px;
    }
    .button-wrapper {
      text-align: center;
      margin-top: 16px;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #e5e5e5;
      color: #666;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="header">
    <img src="<%= URI.join(root_url, 'favicon-32.png') %>" width="22" height="22" alt="ActiveRabbit">
    <h1>🚨 ActiveRabbit Alert</h1>
    <p>Project: <%= @project.name %></p>
  </div>

  <div class="content">
    <div class="alert-body">
      <%= @body %>
    </div>

    <div class="button-wrapper">
      <a href="<%= @project_errors_url %>" class="button">
        View in ActiveRabbit Dashboard
      </a>
    </div>

    <div class="footer">
      <p>
        This alert was generated by ActiveRabbit monitoring for project "<%= @project.name %>".
        <br>
        <a href="<%= @project_settings_url %>">Manage your alerts</a>
      </p>
    </div>
  </div>
</body>
</html>
