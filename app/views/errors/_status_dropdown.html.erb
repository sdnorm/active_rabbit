<div id="status_dropdown">
  <%= form_with model: issue,
                url: error_url,
                method: :patch,
                data: { turbo_frame: "status_dropdown" },
                class: "inline" do |f| %>
    <%= f.select :status,
      options_for_select([['🔴 Open', 'open'], ['🟡 WIP', 'wip'], ['🟢 Closed', 'closed']], issue.status),
      {},
      class: "rounded-full text-sm font-normal px-4 py-2 cursor-pointer
              #{ issue.status == 'open' ? 'bg-red-100 text-red-800' :
                issue.status == 'wip' ? 'bg-yellow-100 text-yellow-800' :
                'bg-green-100 text-green-800' }
              hover:shadow-md transition-all duration-200 border-2
              #{ issue.status == 'open' ? 'border-red-200' :
                issue.status == 'wip' ? 'border-yellow-200' :
                'border-green-200' }",
      onchange: 'this.form.requestSubmit();' %>
  <% end %>
</div>

