# Development overrides for docker-compose.yml
# This file is automatically loaded by docker-compose

version: '3.8'

services:
  web:
    environment:
      # Enable Rails development features
      RAILS_ENV: development
      RACK_ENV: development
      # Disable bootsnap in Docker for better file watching
      DISABLE_BOOTSNAP: 1
      # ActiveRabbit Self-Monitoring Configuration
      ACTIVERABBIT_SELF_MONITOR_TOKEN: d52be1708750a355818dc4a3b511bb208e8519d262ae3281ab1494dd800df220
      ACTIVERABBIT_SELF_MONITOR_PROJECT_ID: 7
    # Enable live reloading
    command: >
      bash -c "
        echo 'Waiting for database...' &&
        bundle exec rails db:prepare &&
        echo 'Building Tailwind CSS...' &&
        bundle exec rails tailwindcss:build &&
        echo 'Starting Rails server...' &&
        bundle exec rails server -b 0.0.0.0 -p 3000
      "

  sidekiq:
    environment:
      RAILS_ENV: development
      RACK_ENV: development
      # ActiveRabbit Self-Monitoring Configuration
      ACTIVERABBIT_SELF_MONITOR_TOKEN: d52be1708750a355818dc4a3b511bb208e8519d262ae3281ab1494dd800df220
      ACTIVERABBIT_SELF_MONITOR_PROJECT_ID: 7
